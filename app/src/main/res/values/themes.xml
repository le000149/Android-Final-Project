<resources>

    <!-- Base application theme. -->
    <style name="Theme.TestSun" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edittext_background</item>
        <item name="android:padding">12dp</item>
        <!-- Add other style properties as needed -->
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:padding">12dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">?attr/selectableItemBackground</item>
    </style>

    <style name="TimeTextStyle" parent="TextAppearance.AppCompat.Large">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/time_text_color</item>
        <!-- Add other style properties as needed -->
    </style>




    <style name="GrayButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/gray</item> <!-- Use an appropriate gray color -->
        <item name="android:textColor">@color/white</item> <!-- Optional: Change text color if needed -->
    </style>
</resources>
